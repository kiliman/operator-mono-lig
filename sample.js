// @ts-nocheck

const func = (a, b, c, d, e, f, s, n) => {
// this is a comment
    if (a++ && b-- || c >= d || e <= f) {
    }
    if (s === 'string' && n !== 999) {
    }
    if (a == b && c != d)
    a ?? b
    a <- b <-- c <== d
    a |> b
    a <| b |> c -> d ==> e

    // if (a++ && b-- || c >= d || e <= f) {
    // }
    // if (s === 'string' && n !== 999) {
    // }
    // if (a == b && c != d)
    // a ?? b
    // a <- b <-- c <== d
    // a |> b
    // a <| b |> c -> d ==> e

  // italic ligatures
  // != == >= <= -> => ==>
  // === !== && || ++ --
  // a <- b <-- c <== d
  // a <| b |> c -> d ==> e
  // a ?? b
  // >=>  <=<
  // <=>
  // >>=  =<<

    a >=> b <=< b
    a <=> b
    a >>=  =<<

  }

  <!-- test -->
